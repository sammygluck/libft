

Functions to create:


1. count how many words there are
2. allocation of memory for full string
3. per word
	4. check length (function taking the string at location x and returns the length of the string until it reaches our point)
	5. allocate memory for length
	6. copy values
	7. null byte
	8. things to keep of track of current index of string + current index of 
4. last word is null
5. return array

- 4:
int	stringlength(char *str, location x, delimiter)
{
	set count = 0
	while(strchr(char[x], delimitter)) returns null  
		add 1 to the count;
		add 1 to x;
	return (count)
}

- 6:


		a = current index of string
		str = newly minted string i
		maybe we can use strlcpy
		so we can pass 1. string 2. index of string 3. new string 4. length of string
		string[index], new_string, length,
https://github.com/Ysoroko/FT_SPLIT_TESTER/blob/master/ft_my_split.c
